# include <stdio.h>
# include <math.h>

int main() {

    float r; 
    float p;

    p = pow(2.0, 8.0);

    r = sqrt(2.0);

    printf("The square root of 2 is %f\n", r);

    printf("2 to the 8th power is %.2f\n", p);

    return(0);
}